# `robot_face_animation.py` の追加リファクタリング計画 (Plan03.md)

## 1. 目的

現在の`samples/robot_face_animation.py`のコードに対し、オブジェクト指向設計の原則（特に「関心の分離」と「カプセル化」）をさらに適用し、各クラスの責務をより明確にすることで、拡張性・再利用性・メンテナンス性を向上させる。

## 2. 改善計画

主要なリファクタリング項目として、以下の3点を提案します。

### 計画1: アニメーションロジックのカプセル化

**現状の問題:**
現在、表情をランダムに変更するロジックは`RobotFaceApp`クラスのメインループに実装されています。これにより、`RobotFaceApp`は「アプリケーションの実行」と「具体的なアニメーションパターンの実装」という2つの責務を持ってしまっています。`RobotFace`クラスは、自身がどう動くかを知らず、外部から指示されるだけの描画用オブジェクトになっています。

**改善案:**
アニメーションのロジックを`RobotFace`クラス内にカプセル化し、`RobotFace`を「自律的に振る舞うことができる」オブジェクトに昇格させます。

*   **`RobotFace`クラスに`animate_step()`メソッドを追加:**
    *   このメソッドは、前回の表情変更からの経過時間を内部で管理し、適切なタイミングで`set_target_mood()`や`set_gaze()`を呼び出します。
*   **`RobotFaceApp`の責務を削減:**
    *   `RobotFaceApp`のメインループは、`face.animate_step()`、`face.update()`、`face.draw()`を呼び出すだけのシンプルな責務に限定されます。

**期待される効果:**
*   `RobotFace`が自己完結したコンポーネントとなり、他のアプリケーションでも同じアニメーションロジックで簡単に再利用できるようになります。
*   `RobotFaceApp`のコードが簡潔になり、アプリケーションの全体的な流れが追いやすくなります。

### 計画2: 出力クラスの責務分離（抽象化）

**現状の問題:**
`DisplayOutput`クラスは、初期化時に「どのデバイス（LCD or OpenCV）が利用可能か」というハードウェア検出ロジックと、「実際に画像を表示する」という描画ロジックの両方を持っています。新しい出力先（例: ファイルに保存する、ネットワーク経由で送信する）を追加したい場合、`DisplayOutput`クラス自体を修正する必要があり、拡張性に乏しい状態です。

**改善案:**
Pythonの`abc`（Abstract Base Classes）モジュールを用いて、`DisplayOutput`の責務を「インターフェースの定義」に限定し、具体的な描画処理はサブクラスに分離します。

*   **`DisplayOutput`を抽象基底クラスに:**
    *   `show(image)`と`close()`という抽象メソッドを持つインターフェースとして定義します。
*   **具象クラスの作成:**
    *   `LcdOutput(DisplayOutput)`: LCDへの表示処理を実装します。
    *   `PreviewOutput(DisplayOutput)`: OpenCVウィンドウへの表示処理を実装します。
*   **ファクトリ関数の導入:**
    *   `main`関数（あるいは新しいファクトリ関数）でハードウェアを検出し、適切な出力クラス（`LcdOutput` or `PreviewOutput`）のインスタンスを生成して`RobotFaceApp`に渡すようにします。

**期待される効果:**
*   **拡張性の向上:** 新しい出力デバイスを追加したい場合、`DisplayOutput`を継承した新しいクラスを作成するだけで対応でき、既存のコードへの影響がなくなります。
*   **単一責任の原則:** 各クラスが持つ責任がより単一になり、コードの理解とテストが容易になります。

### 計画3: グローバル定数のカプセル化

**現状の問題:**
`FACE_BG_COLOR`などの描画に関する定数が、依然としてグローバルスコープに定義されています。これは、関連性の低い場所からでも参照・変更が可能であり、カプセル化が不十分です。

**改善案:**
定数を、それを使用する最も関連性の高いクラスに移動します。

*   **`FACE_BG_COLOR`を`RobotFace`クラスへ:**
    *   顔の背景色は顔の描画に直接関連するため、`LINE_COLOR`と同様に`RobotFace`のクラス変数として移動します。
*   **`SCREEN_WIDTH`, `SCREEN_HEIGHT`, `BG_COLOR`を`RobotFaceApp`へ:**
    *   これらの値は、アプリケーションのウィンドウサイズや最終的な画像のパディング（余白）に関連します。`RobotFaceApp`のコンストラクタで受け取るか、クラス変数として定義し、`RobotFace`の`draw`メソッドに引数として渡すように変更します。

**期待される効果:**
*   **カプセル化の徹底:** グローバルスコープがクリーンになり、各クラスが必要とするデータはクラス自身が保持する、よりオブジェクト指向らしい構造になります。
*   **柔軟性の向上:** `draw`メソッドが描画サイズを引数で受け取るようにすれば、インスタンスごとに異なるサイズの顔画像を描画することも可能になります。