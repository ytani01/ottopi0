# Task.md

## フェーズ 1: conftest.py の導入と共有フィクスチャの移動

- [x] `tests/conftest.py` ファイルを作成する。
- [x] `test_11_cli_cmd.py` から `mock_cli_app` フィクスチャを `tests/conftest.py` に移動する。
- [x] `test_11_cli_cmd.py` から移動したフィクスチャのインポートを削除し、テストが引き続き機能することを確認する。
- [x] 他の CLI テストファイル (例: `test_12_cli_bt.py`, `test_13_cli_webui.py`) をレビューし、同様のモックフィクスチャがあれば `conftest.py` に移動する。
- [x] 必要に応じて、これらのフィクスチャを汎用化し、異なるクライアントタイプや設定をサポートできるようにする。

## フェーズ 2: テストセットアップの `pytest.fixture` への標準化

- [x] `test_01_func_calc.py` を開き、`setup_method` の実装を確認する。
- [x] `Calc` クラスのインスタンス化を `pytest.fixture` に変換する。これにより、テストメソッドはフィクスチャを直接使用できるようになる。
- [x] 変更後、`test_01_func_calc.py` のテストがすべてパスすることを確認する。

## フェーズ 3: URL 構築ヘルパーの一元化

- [x] `test_10_cli_svr.py`, `test_12_cli_bt.py`, `test_13_cli_webui.py` を調査し、URL 構築ロジックが重複しているかどうかを確認する。
- [x] もし重複している場合、`cli_url_builder(host, port, apipath)` のようなヘルパー関数を `tests/conftest.py` または専用のテストユーティリティモジュールに作成する。
- [x] 該当するテストファイル内の重複した URL 構築ロジックを新しく作成したヘルパー関数で置き換える。
- [x] 変更後、関連するテストがすべてパスすることを確認する。

## フェーズ 4: CLI エラーメッセージのアサーションの強化

- [x] `test_11_cli_cmd.py` の `test_cli_exception_handling` のような、例外を処理する CLI テストをレビューする。
- [x] 可能であれば、`"Done." in result.output` のような一般的なアサーションを、より具体的なエラーメッセージのアサーションに置き換える。これには、アプリケーションの CLI 出力にわずかな変更が必要になる場合がある。
- [x] 他の CLI テストファイルについても同様に、エラーメッセージのアサーションを強化する機会があれば実施する。
- [x] 変更後、関連するテストがすべてパスすることを確認する。
