# Tasks02: `EyeState` クラスのリファクタリング実行タスク

`Plan02.md` の計画に基づき、以下のタスクを順番に実行します。

- [x] 1. `samples/robot_face_animation.py` の先頭に必要な `Optional` を `typing` からインポートする。
- [x] 2. `EyeState` クラスから `@dataclass` デコレータを削除し、通常のクラスに変更する。
- [x] 3. `EyeState` クラスに、`Plan02.md`で設計した新しい `__init__` メソッドを実装する。
- [x] 4. `EyeState` クラスに、`copy` メソッドと `__repr__` メソッドを実装する。
- [x] 5. `FaceState` クラスの `left_eye` および `right_eye` フィールド定義で、`default_factory` を新しい `EyeState` の初期化方法に合わせて `lambda: EyeState()` に修正する。
- [x] 6. `RobotFace.MOODS` ディクショナリ内で、閉じている目を定義しているすべての `EyeState(...)` の呼び出しを、新しい `closed_curve` パラメータを使用する形に修正する。
- [x] 7. すべてのコード変更が完了した後、`mise run lint` を実行して、コードのスタイルと品質を検証し、必要に応じて修正する。
- [x] 8. `mise run test` を実行して、リファクタリングによって予期せぬ問題（リグレッション）が発生していないことを確認する。
