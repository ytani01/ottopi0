# ベジェ曲線メソッド化タスク (Tasks05.md)

`Plan05.md`で定義されたリファクタリング計画を、以下の具体的な作業手順に分解します。

---

### 1. `_draw_bezier_curve` メソッドの新規作成

- [x] `RobotFace`クラスに、ベジェ曲線の描画を専門に行うプライベートメソッド `_draw_bezier_curve` を追加する。

### 2. `_draw_eye` メソッドのリファクタリング

- [x] `_draw_eye`メソッド内のベジェ曲線描画ロジックを、`_draw_bezier_curve` メソッドの呼び出しに置き換える。

### 3. `draw` メソッドのリファクタリング

- [x] `draw`メソッド内の口の描画部分にあるベジェ曲線描画ロジックを、`_draw_bezier_curve` メソッドの呼び出しに置き換える。

### 4. 品質確認

- [x] すべての変更後、`mise run lint` を実行して、コードスタイルと型チェックに問題がないことを確認する。
