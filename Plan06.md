# さらなるリファクタリング計画 (Plan06.md)

## 1. 目的

`samples/robot_face_animation.py`のコードに対し、さらなるリファクタリングを適用する。具体的には、ハードコードされた数値（マジックナンバー）や色を定数として抽出し、描画処理と自律動作ロジックを分離することで、可読性、保守性、および再利用性を一層向上させる。

## 2. 背景

現状のコードは多くのリファクタリングを経て改善されているが、依然としていくつかの改善点が残っている。

1.  **マジックナンバー:** 顔のパーツの座標（`eye_y = 45`, `mouth_cx = 50`など）が、描画メソッド内に直接記述されている。これにより、後から顔のレイアウトを微調整する際に、コードの複数の箇所を読んで修正する必要があり、手間がかかる。
2.  **ハードコードされた色:** 眉毛や口の色などが、RGBタプルとして直接コードに埋め込まれている。これにより、カラーテーマの変更が困難になっている。
3.  **責務の混在:**
    *   `draw`メソッドが、輪郭、目、口といった複数のパーツの具体的な描画ロジックをすべて含んでおり、肥大化している。
    *   `RobotFace`クラスが、自身の状態管理と描画だけでなく、「自律的に次の表情や視線を決定する」というアプリケーションロジック (`animate_step`メソッド) も担っている。

## 3. 改善計画

### 計画1: レイアウトと色の定数化

描画メソッド内に分散しているレイアウト用の数値と色を、`RobotFace`クラスのクラス変数（定数）として一元管理する。

1.  **レイアウト定数の作成:**
    顔のパーツの座標やオフセット値を、`LAYOUT`のような名前の辞書またはデータクラスにまとめる。
2.  **カラー定数の作成:**
    ハードコードされている色を、`COLORS`のような名前の辞書にまとめる。
3.  **描画メソッドの修正:**
    `draw`および`_draw_eye`メソッド内で、マジックナンバーとハードコードされた色の代わりに、上記で定義した定数を参照するようにコードを修正する。

### 計画2: 描画処理のメソッド化による責務分離

`draw`メソッドの責務を「各パーツの描画メソッドを呼び出すこと」に限定し、見通しを良くする。

1.  **`_draw_outline`, `_draw_mouth`, `_draw_eyes`メソッドの作成:**
    `draw`メソッドから、輪郭、口、両目の描画ロジックをそれぞれ新しいプライベートメソッドに分離・移管する。
2.  **`_draw_brows`メソッドの作成:**
    `_draw_eye`メソッドから眉毛の描画ロジックを分離し、独立した`_draw_brows`メソッドに移管する。
3.  **`draw`メソッドの再構築:**
    `draw`メソッドの本体を、上記で作成した`_draw_outline`, `_draw_eyes`, `_draw_brows`, `_draw_mouth`を適切な順序で呼び出すだけのシンプルな構成に修正する。

### 計画3: 自律動作ロジックの分離

`RobotFace`クラスから、自律的な動作（アニメーション）のロジックを上位の`RobotFaceApp`クラスに移動させる。

1.  **`RobotFace`クラスの修正:**
    *   `animate_step`メソッドを削除する。
    *   アニメーションタイミングを管理していたインスタンス変数（`_next_mood_time`, `_next_gaze_time`）を削除する。
2.  **`RobotFaceApp`クラスの修正:**
    *   `__init__`メソッドで、アニメーションタイミングを管理する変数を初期化する。
    *   `main`メソッドのループ内で、タイミングをチェックし、`self.face.set_target_mood()`や`self.face.set_gaze()`を呼び出すロジック（元の`animate_step`の中身）を実装する。

---

この計画を実行することで、`RobotFace`クラスはより純粋な描画コンポーネントとなり、デザインの変更やロジックの修正が容易になるなど、メンテナンス性と再利用性が大幅に向上することが期待される。